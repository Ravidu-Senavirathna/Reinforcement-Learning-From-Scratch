import sys, os
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
import Constants

BOX_SIZE    = Constants.BOX_SIZE    # 20px  — one grid cell
COLUMNS     = Constants.COLUMNS     # 40
ROWS        = Constants.ROWS        # 30